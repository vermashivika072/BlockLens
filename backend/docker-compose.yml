version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: certichain-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: certichain_ai

volumes:
  mongodb_data:
